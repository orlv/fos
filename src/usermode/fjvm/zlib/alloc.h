#ifndef _Z_ALLOC_H_
#define _Z_ALLOC_H_

#include <stdio.h>

/*

#include <lib/types.h>

void *malloc(size_t size);
void *calloc(count_t items, size_t size);
void free(void *ptr);

*/

#endif
